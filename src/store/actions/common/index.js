import {
  loader,
  error,
  openModalWindow,
  closeModalWindow,
} from './commonAction';
import * as types from './modalWindowType';

export default { loader, error, openModalWindow, closeModalWindow, types };

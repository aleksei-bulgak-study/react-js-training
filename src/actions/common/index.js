import {
  loader,
  error,
  openModalWindow,
  closeModalWindow,
} from './commonAction';
import * as modalTypes from './modalWindowType';

export { loader, error, openModalWindow, closeModalWindow, modalTypes };
